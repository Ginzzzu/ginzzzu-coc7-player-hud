import { MODULE_ID } from "../constants.js";

export const KEEPER_MONITOR_APP_ID = `${MODULE_ID}-keeper-creation-monitor`;
export const KEEPER_MONITOR_CONTROL = `${MODULE_ID}-creation-monitor-control`;
export const KEEPER_MONITOR_TOOL = `${MODULE_ID}-creation-monitor`;
